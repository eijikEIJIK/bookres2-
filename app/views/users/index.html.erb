<hr>
<%= render 'users/user_info' %>
<hr>
<%= render 'books/new', book_new: @book_new %>
<hr>
<% @users.each do |user| %>
  <%= link_to user_path(user.id) do %>
    <%= image_tag user.get_profile_image %>
  <% end %>
  <p>name：<%= user.name %> <%= link_to "show", user_path(user.id) %></p>
</div>
<% end %>